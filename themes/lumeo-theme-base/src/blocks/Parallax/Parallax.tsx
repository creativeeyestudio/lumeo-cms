import React from 'react'
import { ParallaxProps } from './Parallax.interface'

const Parallax: React.FC<ParallaxProps> = (props: ParallaxProps) => {
  return (
    <div>Parallax</div>
  )
}

export default Parallax