import React from 'react'
import { TextImageProps } from './TextImage.interface'

const TextImage: React.FC<TextImageProps> = (props: TextImageProps) => {
  return (
    <div>TextImage</div>
  )
}

export default TextImage